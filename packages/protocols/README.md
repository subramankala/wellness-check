# protocols

Canonical YAML protocol definitions and parsing helpers.

## Layout

- `schemas/`: versioned protocol YAML documents
- `python/src/protocols_lib`: typed loading helpers

Use this package to keep runtime logic protocol-driven.
