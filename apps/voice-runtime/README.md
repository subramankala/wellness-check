# voice-runtime

WebSocket and media stream runtime for live patient conversations.

## Responsibilities

- Maintains low-latency stream session state
- Handles telephony/websocket ingress
- Forwards transcript events to gateway/safety pipeline

Credential wiring is deferred with explicit TODO markers in code and env.
